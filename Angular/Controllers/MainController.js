//main controller
main.controller("MainController", function ($scope, mainFactory) {
    $scope.attacker;
    $scope.defender;
    $scope.characters = [];

    // $scope.getCharacters = () => {
    //     mainFactory.getAll()
    // };


});
